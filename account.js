(function(){async function start(){if(document.body?.dataset?.page!=='account')return;document.querySelector('[data-account-email]').textContent=ME.email||'—';document.querySelector('[data-account-plan]').textContent=(ME.plan||'free').replace(/^./,x=>x.toUpperCase());const f=document.querySelector('[data-change-password]'),out=document.querySelector('[data-password-result]');f.onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));if(d.new_password!==d.confirm_password){out.textContent='New passwords do not match.';return}const b=f.querySelector('button');b.disabled=true;b.textContent='Changing…';try{await api('/api/account/password',{method:'POST',body:JSON.stringify(d)});out.textContent='Password changed successfully. Other sessions have been signed out.';f.reset()}catch(x){out.textContent=x.message;b.disabled=false;b.textContent='Change password'}}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(start,0));else setTimeout(start,0)})();